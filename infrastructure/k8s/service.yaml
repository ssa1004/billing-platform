apiVersion: v1
kind: Service
metadata:
  name: wallet-platform
  namespace: wallet
  labels:
    app.kubernetes.io/name: wallet-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wallet-platform
  ports:
    - name: http
      port: 8080
      targetPort: http
