apiVersion: v1
kind: ServiceAccount
metadata:
  name: wallet-platform
  namespace: wallet
  annotations:
    # IRSA (IAM Role for Service Account) — AWS 환경에서 S3/Secrets Manager 접근용
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/wallet-platform
