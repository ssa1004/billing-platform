apiVersion: v1
kind: Service
metadata:
  name: billing-platform
  namespace: billing
  labels:
    app.kubernetes.io/name: billing-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: billing-platform
  ports:
    - name: http
      port: 8080
      targetPort: http
